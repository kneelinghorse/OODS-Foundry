import { Meta, Story, Canvas, Markdown } from '@storybook/blocks';
import * as SubscriptionDetailStories from './Subscription.Detail.stories';

<Meta of={SubscriptionDetailStories} />

<Canvas>
  <Story of={SubscriptionDetailStories.CrossProviderDossier} />
</Canvas>

<Markdown>
{`### Notes

- Detail context shifts density automatically; the same markup renders for every provider.
- Sidebar invoices reuse the shared mapping file so chips stay in sync with the list experience.
- Health and usage metrics pull straight from the canonical object produced by the traits.
`}
</Markdown>

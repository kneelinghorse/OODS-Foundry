import { Meta, Story, Canvas, Markdown } from '@storybook/blocks';
import * as InvoiceFormStories from './Invoice.Form.stories';

<Meta of={InvoiceFormStories} />

<Canvas>
  <Story of={InvoiceFormStories.CreditWorkflow} />
</Canvas>

<Markdown>
{`### Notes

- Inputs bind to normalized invoice fields; provider badges remain read only context.
- Credit amounts reuse the same control styling—no hard coded colors or overrides.
- Layout is identical for Stripe and Chargebee, proving the trait contract keeps forms pure.
`}
</Markdown>

import { Meta, Story, Canvas } from '@storybook/blocks';
import * as InvoiceListStories from './Invoice.List.stories';

<Meta of={InvoiceListStories} />

<Canvas>
  <Story of={InvoiceListStories.CollectionsBoard} />
</Canvas>

### What to watch

- Canonical statuses (`posted`, `open`, `past_due`) map to the same tones regardless of provider.
- Provider badges stay as descriptive text; no inline styling or conditional classes are needed.
- Metrics aggregate normalized amounts in minor units to avoid mixed currency drift.
